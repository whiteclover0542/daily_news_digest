# hoya TODO

> 할 일 목록은 맨 위에서 관리하고, 그 아래 날짜별로 오늘 목표 / 오늘 한 일을 기록합니다 (최신순 정렬).

## 할 일
1. [x] 뉴스 소스 조사 및 수집 방식 확정 — MVP 소스를 과학기술정보통신부 보도자료로 확정했다. RSS에서 새 자료를 찾고, 상세 페이지·공개 첨부 PDF/HWPX 전문을 수집해 요약한다. 상세: [`docs/news-source-research.md`](../news-source-research.md)
2. [ ] 수집 공통 규격 공유 — 조사 결과와 수집기 입력 형식을 whiteclover와 공유한다. DB는 PostgreSQL, 카테고리 slug는 `politics`/`economy`/`it`/`sports`/`culture`를 기준으로 한다.
3. [ ] 수집기 구현 — `backend/pipeline/collectors/`에 확정된 뉴스 소스의 RSS/API 수집기를 만들고 제목·언론사·URL·발행일·본문을 공통 형식으로 반환한다.
4. [ ] 전처리 구현 — `backend/pipeline/preprocessing/`에 본문 정제, URL/제목 기준 중복 제거, 광고성·본문 부족 기사 필터링을 구현한다.
5. [ ] LLM 요약·분류 모듈 구현 — `backend/ai/`에 API 클라이언트, 요약/카테고리 분류 프롬프트와 파서를 만들고 실패·형식 오류를 처리한다.
6. [ ] DB 적재 연동 — 처리 결과(원문 정보, 카테고리, 요약, 키워드, 수집 시각)를 기존 `upsert_article()`로 중복 없이 저장하도록 파이프라인을 연결한다.
7. [ ] 파이프라인 실행 흐름 구성 — 수집 → 전처리 → 요약·분류 → DB 적재를 한 번에 실행하는 진입점을 만들고, 단계별 로그와 실패 원인을 남긴다.
8. [ ] 스케줄러 자동 실행 설정 — Render 무료 티어의 슬립을 고려해 GitHub Actions cron을 우선 검토하고, 매일 새벽 실행·재실행 정책을 설정한다.
9. [ ] 테스트 및 운영 문서화 — 수집/전처리/AI 응답 파싱 단위 테스트와 소규모 통합 테스트를 추가하고 환경 변수·실행 방법을 README에 문서화한다.
10. [ ] (확장) 키워드 트렌드 집계 — 일자·카테고리 기준 키워드 빈도를 집계해 프론트 시각화에 전달할 데이터 형식을 설계·구현한다.

---

## 2026-09-03

### 오늘 목표
- [x] 뉴스 소스(RSS/API) 후보를 조사하고, 수집에 필요한 공통 기사 필드를 정리한다.
- 카테고리 목록과 DB 선택 시 파이프라인에 필요한 요구사항을 정리해 whiteclover와 공유한다.

### 오늘 한 일
- 프로젝트 기획서, 진행 로그, API·DB 초안 및 폴더별 역할 문서를 검토했다.
- hoya 담당 파이프라인 작업을 의존성 순서로 TODO에 정리했다.
- 뉴스 소스 조사 완료: 과기정통부·행정안전부·문체부·산업부 보도자료를 비교했다. 전문 이용 근거와 IT 자료 범위를 고려해 과기정통부 보도자료를 MVP 소스로 확정했다.
- 수집 방식 확정: RSS에서 새 자료를 찾고 상세 페이지·공개 첨부 PDF/HWPX 전문을 수집한다. 오전 6시 수집으로 오전 7시 브리핑을 준비하고, 오후 1시에 보강 수집한다.
